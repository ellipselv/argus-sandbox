events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://frontend:5678;
        }

        location /health {
            proxy_pass http://backend:5678;
        }
    }
}
